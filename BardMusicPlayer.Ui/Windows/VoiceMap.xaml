<Window x:Class="BardMusicPlayer.Ui.Windows.VoiceMap"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BardMusicPlayer.Ui.Windows"
        mc:Ignorable="d"
        Closing="Window_Closing"
        Title="OMNI Vision" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="19*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>
        <ScrollViewer x:Name="gScroller" Grid.Row="0" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible" Grid.ColumnSpan="2" >
            <Grid  Name="grid" Background="Black">
                <Canvas x:Name="gCanvasPlot0"  HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,0,0,0" Width="2000"  Height="400" />
            </Grid>
        </ScrollViewer>
        <TextBox x:Name="gOver" Background="WhiteSmoke" VerticalScrollBarVisibility="Visible" Grid.ColumnSpan="2" Margin="0,334,0,0" Grid.RowSpan="2" />
    </Grid>
</Window>
