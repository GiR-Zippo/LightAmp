<UserControl x:Class="BardMusicPlayer.Ui.Controls.SongBrowser"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Locale ="clr-namespace:BardMusicPlayer.Ui.Locales"
             mc:Ignorable="d" 
             Background="{DynamicResource BackgroundBrush}"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition Height="25" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="Path:" HorizontalAlignment="Left"/>
            <TextBox Grid.Column="1" x:Name="SongPath" Text="" VerticalAlignment="Center" HorizontalAlignment="Stretch" PreviewTextInput="SongPath_PreviewTextInput"></TextBox>
            <Button Grid.Column="2" Content="..." Click="Button_Click" Height="18" ToolTip="{x:Static Locale:Language.SongeBrowser_TargetDir}"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Search here ->" HorizontalAlignment="Left"/>
            <TextBox Grid.Column="1" Text="" x:Name="SongSearch" VerticalAlignment="Center" PreviewTextInput="SongSearch_PreviewTextInput"></TextBox>
        </Grid>
        <ListView Grid.Row="2" x:Name="SongbrowserContainer" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" BorderThickness="0" 
                    ScrollViewer.HorizontalScrollBarVisibility="Visible"
                         ScrollViewer.VerticalScrollBarVisibility="Visible"
                         ScrollViewer.CanContentScroll="True">
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Add To Playlist" Click="AddToPlaylist_Click" ToolTip="{x:Static Locale:Language.Global_Load_Song_To_Playlist}"/>
                    <MenuItem Header="Song Preview" Click="LoadSongToPreview" ToolTip="{x:Static Locale:Language.Global_Load_Song_To_Preview}"/>
                    <MenuItem Header="Upload to BMP Midi" Click="UploadToBMP_Click" />
                    <!--<MenuItem Header="Show Information"></MenuItem>-->
                </ContextMenu>
            </ListView.ContextMenu>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Value}"/>
                </DataTemplate>

            </ListView.ItemTemplate>
        </ListView>

        <Popup x:Name="UploadPopup" 
       IsOpen="False" 
       Placement="Center" 
       StaysOpen="True" 
       AllowsTransparency="True">

            <!-- Der sichtbare Kasten mit deinem Design -->
            <Border Background="{DynamicResource BackgroundBrush}" 
            BorderBrush="#444" 
            BorderThickness="1" 
            CornerRadius="6" 
            Padding="15" 
            Width="380">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="15"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <Label   Grid.Row="0" Grid.Column="1" Content="RegEx" Visibility="Hidden"/>
                    <TextBox Grid.Row="0" Grid.Column="2" x:Name="Upload_regex_txt" Visibility="Hidden" VerticalContentAlignment="Center"/>

                    <Label   Grid.Row="1" Grid.Column="1" Content="Artist"/>
                    <TextBox Grid.Row="1" Grid.Column="2" x:Name="Upload_artist_txt" VerticalContentAlignment="Center"/>

                    <Label   Grid.Row="2" Grid.Column="1" Content="Title"/>
                    <TextBox Grid.Row="2" Grid.Column="2" x:Name="Upload_title_txt" VerticalContentAlignment="Center"/>

                    <Label   Grid.Row="3" Grid.Column="1" Content="Source"/>
                    <TextBox Grid.Row="3" Grid.Column="2" x:Name="Upload_source_txt" VerticalContentAlignment="Center"/>

                    <Label   Grid.Row="4" Grid.Column="1" Content="Source Url"/>
                    <TextBox Grid.Row="4" Grid.Column="2" x:Name="Upload_source_url_txt" VerticalContentAlignment="Center"/>

                    <Button Grid.Row="6" Grid.Column="1" Content="Okay" Click="Upload_Okay_Click" Height="25"/>
                    <Button Grid.Row="6" Grid.Column="2" Width="90" HorizontalAlignment="Right" Content="Cancel" Click="Upload_Cancel_Click" Height="25"/>
                </Grid>
            </Border>
        </Popup>
    </Grid>
</UserControl>
